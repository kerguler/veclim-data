services:
  veclim-data-server:
    container_name: veclim-data-server
    build:
      context: .
      args:
        VEC_PORTE: "${VEC_PORTE}"
    image: veclim-data-server
    restart: unless-stopped
    expose:
      - 80
    volumes:
      - "${VEC_DATA}:${DIR_DATA}"
    env_file:
      - .env

  veclim-data-server-nginx:
    image: nginx:stable
    container_name: veclim-data-server-nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - nginx_cache:/var/cache/nginx
    ports:
      - "${VEC_PORTE}:80"
    depends_on:
      - veclim-data-server

volumes:
  nginx_cache:
